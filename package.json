{
  "name": "ics_sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:site": "node scripts/build-site.js",
    "sync:local": "node scripts/run-sync.js",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3"
  }
}
